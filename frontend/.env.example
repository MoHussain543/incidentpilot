VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_API_BASE_URL=http://localhost:8080

# Backend (not used by Vite directly): set SUPABASE_URL to your project URL
# so Spring Boot can verify Supabase access tokens against the project's JWKS.
